# Ralph Progress Log
# Feature: frontend-backend-integration
# Branch: ralph/frontend-backend-integration
# Started: 2026-07-16
# Previous run archived to: archive/2026-07-16-mini-workbuddy-p0/

## Codebase Patterns
- Frontend currently uses Lovable TanStack Start UI with mock-store; real APIs live under backend FastAPI `/api`.
- Prefer `frontend/src/lib/api/*` mappers for snake_case ↔ camelCase; do not persist API keys in localStorage.
- Backend model config id is UUID `id`; supplier model name must be stored in field `model`. Agent Loop must call chat completions with `model`, not display `name`.
- Builtin tools are fixed: `read_file` / `write_file` / `execute_command`.
- Skills are directories under `workspace/config/skills/{id}/SKILL.md`; management API is still missing at start of this feature.
- Chat send is SSE via `POST /api/chat/send`; frontend must stream with fetch and map events to UI parts.
- Only edit `frontend/`, not `frontend-bak/`.

---


## 2026-07-17 04:45 - US-001
- 实现了前端 API client 骨架：`frontend/src/lib/api/{client,types,mappers,index}.ts`，并在 `frontend/vite.config.ts` 增加 `/api` 代理到 `http://127.0.0.1:8000`（支持 `VITE_API_TARGET` 覆盖）。
- `client.ts`：统一 `request<T>()` + `apiClient` 动词封装；非 2xx 抛 `ApiError`（含 status、可读 detail、原始 body）；解析 FastAPI `{"detail":...}`；网络失败 status=0；204 返回 null。
- `types.ts`：对齐后端 Pydantic schemas（model/tool/agent/conversation/ping），保持 snake_case 线格式。
- `mappers.ts`：显式 `mapModel`/`mapAgent` 做 snake→camel，`toSnakeCase` 通用工具供请求体。
- `index.ts`：统一出口 `@/lib/api`。
- 更改的文件：frontend/vite.config.ts, frontend/src/lib/api/client.ts, frontend/src/lib/api/types.ts, frontend/src/lib/api/mappers.ts, frontend/src/lib/api/index.ts
- 验证：tsc --noEmit 通过；`eslint src/lib/api` 通过（根目录其它文件的 800 条 prettier 报错为既有问题，非本 story 引入）。复用已在 5178 端口运行的 mini-workbuddy dev server + 8000 后端：`curl http://127.0.0.1:5178/api/ping` 返回 `{"pong":"ok"}`，代理生效。
- 未修改任何业务页面 mock 数据源。
- **未来迭代的学习：**
  - pattern：复用 `@/lib/api` 出口，新增端点在 `types.ts` 加 wire 类型 + `mappers.ts` 加 `mapX`，不要在页面内手写 snake/camel 转换。
  - pattern：client 基于相对 `/api` 前缀，靠 Vite proxy 转发，永不硬编码 host；dev server 端口不固定，验证前先 `ss -ltnp` 找出真正属于 mini-workbuddy 的 vite 进程（cmd 含 `mini-workbuddy/frontend`）。
  - 陷阱：5173 端口被另一个项目（enzebaba-cloud）的 vite 占用；5174 也被 ragent-plus 占用。mini-workbuddy 自己的 dev server 实际在 5178（以及 5180/5181/5182 等历史实例仍在跑）。验证代理务必用 `ps -o args= -p <pid>` 确认进程归属。
  - 陷阱：根目录 `npm run lint` 报 800 条 prettier 错误是仓库既有状态，非本次引入；质量门只需保证新增/改动文件 lint 干净（`eslint src/lib/api`）。
  - workflowMode 未设置 → 默认 `develop_and_validate`，开发阶段不置 `passes=true`，交由 Validator。
---


## 2026-07-17 05:09 - US-002
- 实现了后端模型配置的供应商 `model` 字段与 `is_default` 默认模型标记。
- `schemas/model.py`：`ModelBase` 增加 `model:str`（必填，1-200，供应商模型名）与 `is_default:bool`（默认 False）；`ModelCreate/ModelUpdate/ModelRead` 自动继承。
- `services/models_store.py`：新增 `get_default_model()` 与 `set_default_model(id)`——后者在单次 filelock 写入内清除其余 `is_default`，保证“全局至多一个默认”不变式；新增私有 `_now_iso()` 避免 store 反向依赖 schema。
- `api/models.py`：POST/PUT 存取 `model`/`is_default`；新增 `PUT /api/models/{id}/default` 端点；`_apply_default_flag()` 在创建时按需触发 set_default。
- `services/model_tester.py` & `services/agent_loop.py`：调用 chat/completions 时改用 `model.get("model") or model.get("name")`，不再用展示 name。
- 更改的文件：backend/app/schemas/model.py, backend/app/services/models_store.py, backend/app/api/models.py, backend/app/services/model_tester.py, backend/app/services/agent_loop.py, backend/tests/test_models_api.py, backend/tests/test_models_test_api.py, backend/tests/test_chat_send_api.py
- 验证：`uv run pytest` 145 passed；`uv run mypy app` Success（34 文件）。新增/补充测试覆盖 model 必填、is_default 互斥、PUT default、明文密钥不泄露、probe 使用供应商模型名等 AC。
- 读取响应永不包含明文 api_key（ModelRead 仅暴露 api_key_ref/api_key_env）。
- **未来迭代的学习：**
  - pattern：默认模型互斥逻辑放在 `models_store.set_default_model` 单次 locked write，API 层只做调用与 404 判断；保持 store 是唯一写 models.json 的层。
  - pattern：store 层不 import schema（避免循环），用本地 `_now_iso()`；schema 也有公共 `now_iso()`，二者格式一致。
  - 陷阱：`model_tester` / `agent_loop` 历史上用展示 `name` 当 model 字段发请求，必须显式改成 `model.get("model") or model.get("name")`，否则旧种子数据无 model 时回退 name。
  - workflowMode 未设置 → develop_and_validate，不置 passes=true，交由 Validator。
---
