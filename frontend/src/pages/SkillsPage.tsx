import { Sparkles } from 'lucide-react'
import PlaceholderPage from './PlaceholderPage'

export default function SkillsPage() {
  return (
    <PlaceholderPage
      title="技能管理"
      description="管理技能，支持新增、编辑与导入"
      icon={<Sparkles size={40} />}
    />
  )
}
